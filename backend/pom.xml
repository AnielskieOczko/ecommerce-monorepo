<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.8</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.rj</groupId>
    <artifactId>ecommerce_backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>ecommerce_backend</name>
    <description>ecommerce backend spring boot application</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Configuration Processor -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>8.2.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <!-- https://mvnrepository.com/artifact/org.flywaydb/flyway-mysql -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
            <version>11.3.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.11.5</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.3.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope> <!-- Or compile if you want to use Micrometer API directly -->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>7.8.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${project.basedir}/../api-specifications/ecommerce-openapi.v1.yaml</inputSpec>
                            <!-- No longer needed as we use configOptions primarily -->
                            <!-- <configurationFile>${project.basedir}/openapi-generator-config.json</configurationFile> -->
                            <!-- Change generatorName and add library -->
                            <generatorName>java</generatorName>
<!--                            <library>spring-boot</library>-->
                            <output>${project.build.directory}/generated-sources/openapi</output> <!-- Keep output path -->
                            <!-- Define distinct packages for generated code -->
                            <apiPackage>com.rj.ecommerce_backend.generated.api</apiPackage> <!-- Keep package names -->
                            <modelPackage>com.rj.ecommerce_backend.generated.model</modelPackage> <!-- Keep package names -->
                            <!-- Remove spring generator specific options -->
                            <!-- <supportingFilesToGenerate>ApiUtil.java</supportingFilesToGenerate> -->
                            <!-- <globalProperties>...</globalProperties> -->
                            <configOptions>
                                <!-- Keep relevant options -->
                                <useSpringBoot3>true</useSpringBoot3>
                                <useJakartaEe>true</useJakartaEe>
                                <openApiNullable>false</openApiNullable>
                                <sourceFolder>src/gen/java</sourceFolder> <!-- Standard source folder name -->
                                <generateModels>true</generateModels>
                                <generateApis>false</generateApis> <!-- Assuming you only want models -->
                                <generateApiTests>false</generateApiTests>
                                <generateModelTests>false</generateModelTests>
                                <useBeanValidation>true</useBeanValidation>
                                <serializationLibrary>jackson</serializationLibrary>
                                <useRecords>true</useRecords> <!-- Keep this! -->
                                <hideGenerationTimestamp>true</hideGenerationTimestamp>
                                <dateLibrary>java8</dateLibrary>
                                <java8>true</java8> <!-- Keep if using java8 dateLibrary -->
                                <!-- Remove options specific to 'spring' generator or less relevant for 'java' generator -->
                                <!-- <delegatePattern>true</delegatePattern> -->
                                <!-- <interfaceOnly>true</interfaceOnly> -->
                                <!-- <useTags>true</useTags> -->
                                <!-- <useOptional>true</useOptional> --> <!-- Can keep if desired, but test without first -->
                                <!-- <skipDefaultInterface>true</skipDefaultInterface> -->
                                <!-- <useSwaggerUI>false</useSwaggerUI> -->
                                <!-- <useSpringfox>false</useSpringfox> -->
                                <!-- <recordWithEqualsHashCode>true</recordWithEqualsHashCode> --> <!-- useRecords implies this -->
                            </configOptions>
                            <!-- Specify only the models this service needs -->
                            <modelsToGenerate>Money,Address,PhoneNumber,ImageInfo,OrderStatus,PaymentStatus,PaymentMethod,ShippingMethod,CustomerInfo,ProductSummary,OrderItem,Category,Product,ProductCreateRequest,ProductUpdateRequest,CartItem,Cart,CartItemAddRequest,Order,OrderCreateRequest,OrderStatusUpdateRequest,UserInfo,UserCreateRequest,UserUpdateRequest,ChangePasswordRequest,AuthResponse,PaymentRequest,PaymentResponse,PaymentNotification,PaymentLineItem</modelsToGenerate>
                            <modelNameSuffix>DTO</modelNameSuffix> <!-- Keep suffix -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.5.0</version> <!-- Use the latest stable version -->
                <executions>
                    <execution>
                        <id>add-openapi-generated-source</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <!-- Adjust path if generator output changes -->
                                <source>${project.build.directory}/generated-sources/openapi/src/main/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
