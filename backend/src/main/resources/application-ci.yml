# =============================================================================
# CI PROFILE OVERRIDES
# For Continuous Integration (e.g., GitHub Actions)
# =============================================================================
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ecommerce_ci
    username: testuser
    password: ${SPRING_DATASOURCE_PASSWORD}
  flyway:
    url: ${spring.datasource.url}
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
  jpa:
    hibernate:
      ddl-auto: none

app:
  storage:
    location: /tmp/ecommerce/test-images-ci